<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>הרשמה ללקוחות</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light text-center">
    <div class="container mt-5">
        <h1 class="text-success">הרשמה למערכת</h1>
        <p class="lead">מלא את הפרטים שלך כדי להתחיל</p>

        <% if (error) { %>
            <p class="text-danger"><%= error %></p>
        <% } %>

        <form method="POST" action="/customers/signup" class="text-start mx-auto w-50 p-4 bg-white shadow rounded">
            <div class="mb-3">
                <label for="name" class="form-label fw-bold">שם מלא</label>
                <input type="text" id="name" name="name" class="form-control" required>
            </div>

            <div class="mb-3">
                <label for="phone" class="form-label fw-bold">טלפון</label>
                <input type="text" id="phone" name="phone" class="form-control" pattern="^[0-9]{10}$" title="יש להזין מספר טלפון בן 10 ספרות" required>
            </div>

            <div class="mb-3">
                <label for="email" class="form-label fw-bold">אימייל</label>
                <input type="email" id="email" name="email" class="form-control">
            </div>

            <div class="mb-3">
                <label for="model" class="form-label fw-bold">דגם רכב</label>
                <input type="text" id="model" name="model" class="form-control" required>
            </div>

            <div class="mb-3">
                <label for="license_plate" class="form-label fw-bold">מספר רכב</label>
                <input type="text" id="license_plate" name="license_plate" class="form-control" required>
            </div>

            <button type="submit" class="btn btn-success w-100">📌 הרשמה</button>
        </form>
    </div>
    <div id="successMessage" class="alert alert-success d-none"></div>
    <div id="errorMessage" class="alert alert-danger d-none"></div>
    
    <script>
        document.querySelector("form").addEventListener("submit", function(event) {
            event.preventDefault();
    
            const formData = {
                name: document.getElementById("name").value,
                phone: document.getElementById("phone").value,
                email: document.getElementById("email").value,
                model: document.getElementById("model").value,
                license_plate: document.getElementById("license_plate").value
            };
    
            fetch("/customers/signup", {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(formData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    document.getElementById("successMessage").textContent = data.message;
                    document.getElementById("successMessage").classList.remove("d-none");
                    setTimeout(() => window.location.href = "/customers/login", 2000);
                } else {
                    document.getElementById("errorMessage").textContent = data.message;
                    document.getElementById("errorMessage").classList.remove("d-none");
                }
            })
            .catch(error => console.error("❌ שגיאה בהרשמה:", error));
        });
    </script>
    <script>
        document.querySelector("form").addEventListener("submit", function(event) {
            event.preventDefault();
            const formData = new FormData(this);
            fetch("/customers/signup", {
                method: "POST",
                body: formData
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    alert("✅ נרשמת בהצלחה! כעת ניתן להתחבר.");
                    window.location.href = "/customers/login";
                } else {
                    alert("❌ שגיאה בהרשמה: " + data.message);
                }
            })
            .catch(error => console.error("שגיאה בהרשמה:", error));
        });
    </script>

</body>
</html>
