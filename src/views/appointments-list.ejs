<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title>רשימת התורים שלי</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light text-center">
    <div class="container mt-5">
        <h1 class="text-success">רשימת התורים שלי</h1>
        <p class="lead">כל התורים שנקבעו עבורך</p>

        <div class="card p-4 shadow-lg bg-white w-75 mx-auto">
            <ul class="list-group">
                <% if (appointments.length === 0) { %>
                    <li class="list-group-item text-danger">אין לך תורים קבועים</li>
                <% } else { %>
                    <% appointments.forEach(function(appointment) { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span>🚗 <strong><%= appointment.service %></strong> | 📅 <%= appointment.date %></span>
                            <form method="POST" action="/customers/cancel-appointment" class="d-inline">
                                <input type="hidden" name="appointment_id" value="<%= appointment.id %>">
                                <button type="submit" class="btn btn-outline-danger btn-sm">בטל תור</button>
                            </form>
                        </li>
                    <% }); %>
                <% } %>
            </ul>
        </div>
    </div>
</body>
</html>
