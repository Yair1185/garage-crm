<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-4xl mx-auto bg-white shadow-lg p-6 rounded-lg">
        <h1 class="text-3xl font-bold mb-6">ניהול תורים</h1>

        <table class="w-full border-collapse">
            <thead>
                <tr>
                    <th class="border-b p-2">רכב</th>
                    <th class="border-b p-2">מספר רישוי</th>
                    <th class="border-b p-2">שירות</th>
                    <th class="border-b p-2">תאריך</th>
                    <th class="border-b p-2">סטטוס</th>
                    <th class="border-b p-2">עדכון</th>
                    <th class="border-b p-2">ביטול</th>
                </tr>
            </thead>
            <tbody>
                <% appointments.forEach(function(appointment) { %>
                    <tr class="text-center">
                        <td class="border-b p-2"><%= appointment.model %></td>
                        <td class="border-b p-2"><%= appointment.license_plate %></td>
                        <td class="border-b p-2"><%= appointment.service %></td>
                        <td class="border-b p-2"><%= appointment.date %></td>
                        <td class="border-b p-2"><%= appointment.status %></td>
                        <td class="border-b p-2">
                            <form method="POST" action="/manager/appointments/update">
                                <input type="hidden" name="appointment_id" value="<%= appointment.id %>">
                                <select name="status" class="border p-1 rounded">
                                    <option value="מתוזמן" <%= appointment.status === 'מתוזמן' ? 'selected' : '' %>>מתוזמן</option>
                                    <option value="הושלם" <%= appointment.status === 'הושלם' ? 'selected' : '' %>>הושלם</option>
                                </select>
                                <button type="submit" class="bg-blue-500 text-white px-3 py-1 rounded hover:bg-blue-600">עדכן</button>
                            </form>
                        </td>
                        <td class="border-b p-2">
                            <form method="POST" action="/manager/appointments/cancel">
                                <input type="hidden" name="appointment_id" value="<%= appointment.id %>">
                                <button type="submit" class="bg-red-500 text-white px-3 py-1 rounded hover:bg-red-600">בטל</button>
                            </form>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
